import styled from "styled-components";

const Flag = styled.i`
  display: block;
  position: absolute;
  font-size: 100px;
  right: -10px;
  bottom: -16px;
  opacity: 0.7;
  transform: rotate(-15deg);
`;

export default Flag;
